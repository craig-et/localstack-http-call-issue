{
  "name": "http-call-issue",
  "version": "1.0.0",
  "scripts": {
    "deploy": "sls deploy --stage local --region ap-southeast-2 --config serverless.yaml",
    "test": "node lambda-invocation-tests.js",
    "format": "npx --yes prettier@3 './**/*.js' --write"
  },
  "devDependencies": {
    "@aws-sdk/client-lambda": "^3.567.0",
    "serverless": "^3.3.0",
    "serverless-localstack": "1.0.3"
  }
}
